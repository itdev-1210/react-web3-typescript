export interface Address {
    3?: string
    1: string
}

export interface PoolConfig {
    id: number
    apy: number // percent
    price: number
    duration: number // days
    startTime: number
    endTime: number
}